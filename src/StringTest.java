public class StringTest {
    /*
       length
       isEmpty
       charAt
       codePointAt
       equals
       equalsIgnoreCase
       compareTo
       compareToIgnoreCase
       startsWith
       endsWith
       indexOf
       lastIndexOf
       substring
       subSequence
       concat
       replace
       contains
       split
       join 1.8
       toLowerase
       toUpperCase
       trim
       strip  11
       isBlank
       format
       valueOf
       repeat 11
   */
}
